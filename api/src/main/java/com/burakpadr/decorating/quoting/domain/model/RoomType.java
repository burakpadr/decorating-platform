package com.burakpadr.decorating.quoting.domain.model;

/** Room types the price book carries a {@link RoomTypeConfig} for (§5.3). */
public enum RoomType {
	LIVING_ROOM,
	MASTER_BEDROOM,
	BEDROOM,
	STUDY,
	KITCHEN,
	BATHROOM,
	HALLWAY,
	BALCONY
}
