/**
 * JPA entities, repository implementations, and explicit domain↔entity mappers.
 *
 * <p>JPA entities are separate classes from domain models. No {@code @ManyToOne} may cross
 * a module boundary.
 */
package com.burakpadr.decorating.quoting.adapter.out.persistence;
