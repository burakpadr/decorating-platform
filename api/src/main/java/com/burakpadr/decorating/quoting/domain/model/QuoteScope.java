package com.burakpadr.decorating.quoting.domain.model;

/** Whether the whole home is being painted or only some of it ({@code quote_request.scope}). */
public enum QuoteScope {
	WHOLE_HOME,
	SELECTED_ROOMS
}
