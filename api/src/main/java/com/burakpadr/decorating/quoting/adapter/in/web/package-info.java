/**
 * REST controllers, request/response DTOs, and mappers.
 *
 * <p>Operator responses carry total_cost and margin_ratio. Customer-facing responses use
 * separate DTO types — never conditional field stripping on a shared type.
 */
package com.burakpadr.decorating.quoting.adapter.in.web;
