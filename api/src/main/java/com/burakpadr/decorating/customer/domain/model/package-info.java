/**
 * The customer module's domain model.
 *
 * <p>No framework annotations here, as everywhere else under {@code domain}. A row exists only after
 * a phone number is proved (§4.1); everything before that is a draft in {@code quoting}.
 */
package com.burakpadr.decorating.customer.domain.model;
